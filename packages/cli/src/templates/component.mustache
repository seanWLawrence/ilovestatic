// @flow

import React, { Component } from 'react';

type Props = {
  {{#props}}
  {{name}}: {{type}}, 
  {{/props}}
};

export default class {{name}} extends Component<Props, {}> {

  render() {
    let {
      {{#props}}
      {{name}},
      {{/props}}
    } = this.props;

    return (
      <>
        New component
      </>
    )
  }
};